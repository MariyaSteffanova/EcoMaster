@using Kendo.Mvc;
@{
    ViewBag.Title = "_HomeStorageNavigation";
    // Layout = "~/Areas/Private/Views/Shared/_HomeStoragePanelLayout.cshtml";
}

<div id="homestorage-nav">
    <nav class="navbar navbar-default" role="navigation">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-side-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Brand</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-side-collapse">
            <ul class="nav navbar-nav">
                <li class="active"><a href="@Url.Action("Index", "HomeStorage", new { area = "Private"})">Storage</a></li>
                <li><a href="@Url.Action("Index", "Kitchen", new { area = "Private"})">Kitchen</a></li>
                <li><a href="@Url.Action("Index", "Flatmates", new { area = "Private"})">Flatmates</a></li>
                <li><a href="@Url.Action("Index", "Search", new { area = "Private"})">Search products</a></li>
            </ul>
           @Html.Partial("_SearchBarcodeKeyboardField")
        </div><!-- /.navbar-collapse -->
    </nav>

</div>
